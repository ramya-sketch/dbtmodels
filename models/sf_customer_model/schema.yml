version: 2

models:
  - name: stg_orders
    columns:
      - name: ORDER_ID
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn

      - name: CUSTOMER_ID
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn

  - name: stg_customer
    columns:
      - name: CUSTOMER_ID
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn
