version: 2

models:
  - name: product_sales_vw
    tags: ["production", "analytics"]  # Corrected tag placement
    description: "A view showing product sales data"
    columns:
      - name: PRODUCTID
        description: "The unique identifier for the product"
        tests:
          - not_null
          # - unique

  - name: employee_sales_vw
    tags: ["production", "analytics"]  # Corrected tag placement
    description: "A view showing employee sales data"

