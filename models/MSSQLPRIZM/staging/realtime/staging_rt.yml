version: 2

sources:
  - name: dbt_raw          # <-- must match what your model uses
    database: DQLABS
    schema: RAW_DBT        # <-- your actual schema in Snowflake
    tables:
      - name: customers_stream
      - name: orders_stream
      - name: payments_stream
